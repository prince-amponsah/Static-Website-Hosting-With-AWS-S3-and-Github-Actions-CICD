name: Lint
on: push

jobs:
  build:
    name: Lint
    runs-on: ubuntu-latest
   
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Super-Linter
        uses: super-linter/super-linter@v7.1.0
            
